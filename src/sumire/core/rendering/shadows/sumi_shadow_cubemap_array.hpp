#pragma once

/*
*
* TODO: Implementation of omnidirectional shadow maps as a fixed-dimensions cubemap array.
* 
* Useful for rending point lights. 
*   The alternative is to render the six cube faces into a texture array / atlas / VSM.
*
*/

namespace sumire {

    class SumiShadowCubemapArray {
    public:
        SumiShadowCubemapArray();
    private:
    };

}
